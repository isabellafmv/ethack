// Persistent, impossible-to-miss amber badge whenever the app is running on
// synthetic data. "Someone will demo this by accident" -- not optional.
export function FixtureBadge() {
  return <div className="fixture-badge">FIXTURE DATA</div>;
}
